{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "tinycast-headroom",
  "version": "0.1.1",
  "title": "Headroom",
  "description": "Memory pressure, swap and Claude Code sessions, with a safe reap for idle ones",
  "icon": "assets/headroom.svg",
  "author": "rodrigoalegria",
  "license": "MIT",
  "categories": [
    "Developer Tools",
    "System"
  ],
  "platforms": [
    "macOS"
  ],
  "commands": [
    {
      "name": "index",
      "title": "Headroom",
      "description": "Memory pressure, Claude Code sessions and heavy apps",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "reapPath",
      "type": "textfield",
      "title": "Custom claude-reap Path",
      "description": "Leave empty to use the claude-reap bundled with Headroom",
      "default": "",
      "required": false,
      "placeholder": "~/bin/claude-reap"
    },
    {
      "name": "idleThreshold",
      "type": "dropdown",
      "title": "Idle Threshold",
      "description": "A session idle longer than this can be reaped",
      "default": "2d",
      "required": false,
      "data": [
        {
          "title": "12 hours",
          "value": "12h"
        },
        {
          "title": "1 day",
          "value": "1d"
        },
        {
          "title": "2 days",
          "value": "2d"
        },
        {
          "title": "3 days",
          "value": "3d"
        },
        {
          "title": "7 days",
          "value": "7d"
        }
      ]
    },
    {
      "name": "linearWorkspace",
      "type": "textfield",
      "title": "Linear Workspace",
      "description": "Your Linear workspace slug (linear.app/<slug>/…). Leave empty to hide Open in Linear.",
      "default": "",
      "required": false,
      "placeholder": "my-team"
    }
  ],
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test tests/*.test.ts",
    "install-local": "node scripts/install-local.mjs",
    "package": "node scripts/package.mjs"
  },
  "dependencies": {
    "@raycast/api": "^1.104.0",
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.10",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
